.post-photos
  - post.post_photo.each do |f|
    %img{:src => url_for(:controller => "post_photos", :action => "photo", :id => post.id, :photo_id => f.id, :version => 'square', :filename => File.basename(f.photo.to_s)), :width => 75, :height => 75}
